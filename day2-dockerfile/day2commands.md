# Day 2 Commands

Build the image:

```bash
docker build -t numpy-app .
```

Run the container:

```bash
docker run numpy-app
```

List local images:

```bash
docker images
```

Show image history:

```bash
docker history numpy-app
```